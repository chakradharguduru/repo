-r requirements.txt

# Test suite (tests/) and eval harness (scripts/)
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-mock>=3.14.0
# fastapi.testclient.TestClient needs httpx explicitly — not a hard
# dependency of fastapi/starlette itself, and CI installs from a clean
# environment where it won't already be present transitively.
httpx>=0.27.0
